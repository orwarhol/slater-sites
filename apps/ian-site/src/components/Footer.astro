---
const today = new Date();
---

<div class="site-footer">
	<div class="prefooter">
		<a href="https://www.linkedin.com/in/iancslater/" target="_blank" rel="noopener noreferrer">Connect with me on LinkedIn!</a>
	</div>
	<div class="footer-inner-divider" aria-hidden="true"></div>
	<footer>
		&copy; {today.getFullYear()} Ian Slater. All rights reserved.
	</footer>
</div>
<style>
	.site-footer {
		margin-top: 3rem;
		border-top: 1px solid var(--border-faint);
		background: var(--surface-1);
		padding: 2.25rem 0;
	}
	
	.prefooter,
	footer {
		padding: 1.25rem 1rem;
		color: var(--muted);
		text-align: center;
	}
	
	.prefooter {
		font-size: 0.95em;
	}
	
	.footer-inner-divider {
		height: 1px;
		width: min(520px, calc(100% - 2rem));
		background: var(--border-faint);
		opacity: 0.45;
		margin: 0 auto;
	}
	
	.prefooter a,
	.prefooter a:visited {
		color: var(--pink);
		text-decoration: none;
		transition: all 0.2s ease;
	}
	
	.prefooter a:hover,
	.prefooter a:focus-visible,
	.prefooter a:visited:hover,
	.prefooter a:visited:focus-visible {
		text-decoration: underline;
		text-decoration-color: var(--pink);
		text-underline-offset: 0.2em;
		text-decoration-thickness: 2px;
		filter: drop-shadow(0 0 8px var(--pink));
		outline: none;
	}
</style>
